#include<stdio.h>
#include<string.h>
#define n 100
int stack[n],top=-1;
void push (int value)
{
if (top>=n-1){
printf("push not possible\n");
}
else
}
int main(){
char p[100], value;
gets(p);
int l, num1,num2,i;
for(i=0;i<l; i++);
{
if (p[i]>='0' && p[i]<='9')
{ push(p[i]-'0');

 }
else
}
num2=pop();
num1=pop();
switch (p)
}






